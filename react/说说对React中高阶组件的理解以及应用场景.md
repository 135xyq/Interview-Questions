# 说说对React中高阶组件的理解以及应用场景

## 概念

在React中高阶组件本质上就是一个函数, 该函数接受一个组件并返回一个新的组件

返回的新组件并不会改变原组件的渲染内容, 而是增强其功能:

- 为原组件传入一些额外属性
- 在原组件指定生命周期函数触发时, 做一些事情

作用:

  高阶组件主要是用来分离和封装组件间与组件自身状态无关的通用逻辑, 让通用逻辑能更好地在组件件复用

## 应用场景

- 组件的日志记录
- 权限控制
- 统计上报

